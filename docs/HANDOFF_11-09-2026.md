# Handoff — 11/09/2026

> Sessão: dois ajustes visuais no formulário de Nova Ocorrência + deploy agrupado
> no Netlify (incluindo o toast do BI que estava commitado desde 10/09).

## 1) O que mudou (commit `1544cda`, pushed)

- **Dropdown de tipos sem "— Leve" no macro "9. Registro do RH"**
  (`src/components/ocorrencias/ocorrencia-form/TipoOcorrenciaSection.tsx`):
  tipos desse grupo com gravidade Leve passam a exibir só o nome
  ("Registro Negativo", "Anotações sobre Demissão", "Outros").
  Escopo exato pedido pela gestão: **somente o grupo 9** — nos demais macro
  grupos o sufixo "— Leve" permanece, e dentro do grupo 9 os tipos de
  gravidade Positiva seguem mostrando "— Positiva". A gravidade continua
  visível no quadro abaixo após a seleção do tipo (nada foi removido dali).
- **Dropdown de macro grupo sem scroll**
  (`src/components/ocorrencias/ocorrencia-form/MacroGrupoSection.tsx`):
  `SelectContent` com `max-h-[26rem]` (era o padrão `max-h-72`, ~6 itens) —
  os 9 macro grupos aparecem de uma vez.

## 2) Deploy

- Build (`npm run build`) ok; lint e `tsc` ok antes do commit.
- Deploy de produção no Netlify no site **`plena-corh`** (nunca em
  `sweet-nasturtium-fa7dc4`), verificação do hash do bundle: ver item 3.
- **Este deploy também levou o toast de erro melhorado do botão
  "Atualizar agora" da página PerformanceLab** (`BiPerformanceLabPage.tsx`),
  que estava commitado desde 10/09 sem deploy. Não há mais nada commitado
  pendente de deploy.

## 3) Verificação pós-deploy

- Deploy `6aa3fa7601621f4397eb9474` em `plena-corh.netlify.app` concluído em ~29s.
- Hash do bundle em produção **idêntico** ao do `dist/` local:
  `assets/index-CGA-lBwS.js` (armadilha dos dois sites descartada).

## Pendências / observações

- `func-deployada.txt` (untracked, artefato local) ficou de fora de propósito.
- Próxima verificação de rotina: conferir se o cron `sync-performancelab-diario`
  das 05h00 BRT rodou (`bi_sync_log`) e se o dia anterior já estava publicado
  pelo PL nesse horário (ressalva do handoff de 10/09).
