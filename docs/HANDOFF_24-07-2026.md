# HANDOFF — 24/07/2026 (dia inteiro)

> **Para o próximo agente:** leia também `docs/CONTINUAR_AQUI.md` (sempre atualizado).
> **⚠️ URGENTE PARA AMANHÃ (25/07):** os créditos do Netlify **acabaram** mesmo após as otimizações de hoje — a usuária precisou comprar mais. Tratar com prioridade máxima (ver §1).

---

## 1. ⚠️ Netlify — consumo de créditos (URGENTE)

**O que foi feito hoje:**
- Builds automáticos **desligados** (Site configuration → Build & deploy → "Stop builds"). Deploys agora são manuais.
- **Deploy local configurado:** `npm run deploy:netlify` (script no `package.json`) — builda na máquina da usuária e sobe só o `dist/` pronto via Netlify CLI. **Não consome os 15 créditos de build.** Login do CLI já feito na máquina dela.
- Arquivos pesados e **sensíveis** removidos de `public/` (planilhas, PDF com CPF de todos os colaboradores — risco LGPD, estava público!). Dados agora em `dados-locais/` (gitignored), assets de design em `docs/assets/`.
- `dist/` caiu para ~5,6 MB. Commit `602039d`.
- Descobertos **3 sites antigos ainda publicados** na conta: `plena-adicionais`, `plena-ceu`, `plena-vr` (projetos pré-unificação, deploys de fim de maio). A usuária optou por **mantê-los por ora**.

**O que aconteceu mesmo assim:** os créditos **acabaram** no mesmo dia e ela comprou mais. Ou seja: o consumo principal **não era build** — é **banda/tráfego**.

**Para amanhã (com um agente, urgente):**
1. Abrir o **Analytics/observabilidade** do Netlify e identificar **qual site e qual asset** consome banda (o painel mostrava dezenas de GB em janelas curtas).
2. Avaliar seriamente **pausar/excluir os 3 sites legados** (`plena-adicionais`, `plena-ceu`, `plena-vr`) — suspeitos óbvios de tráfego residual.
3. Verificar se há bots/scrapers batendo nos assets públicos grandes (`pdf.worker.min.mjs` 1,2 MB é servido sem cache imutável — candidato a header de cache longo).
4. Considerar alternativas se o consumo for orgânico: Cloudflare Pages/Workers (banda grátis), ou upgrade de plano.
5. **Não reativar builds automáticos.**

---

## 2. Estabilidade Kimi Code / VS Code (manhã)

- CLI atualizado 0.29.0 → **0.29.1** (instalação nativa Windows não auto-atualiza; comando manual: `irm https://code.kimi.com/kimi-code/install.ps1 | iex`).
- Diagnóstico dos "fechamentos": erros de rede no login (22/07) + extension host do VS Code. Sem crashes reais do CLI nos logs.
- `settings.json` do VS Code criado com `extensions.experimental.affinity` — extensão do Kimi roda em **processo separado** (crash de outra extensão não derruba o painel).
- Modo YOLO ativo (config já tinha `default_permission_mode = "yolo"`; regras `ask` protegem `rm -rf`, git, `supabase db push/deploy`).

## 3. PDF de ocorrência — empresa e CPF (pendência de ontem, RESOLVIDA)

- **Causa raiz confirmada:** o service worker do PWA servia JS antigo. Fix: `skipWaiting()` em `src/sw.ts` — próximas atualizações ativam sozinhas.
- Marcador de build no rodapé do PDF (`build 2026-07-24`) para diagnóstico futuro.
- CPF do colaborador não saía no PDF gerado após "Nova ocorrência" (`COLUNAS_AUTOCOMPLETE` sem `cpf`) — corrigido.
- **Validado pela usuária em produção:** logo, nome/CNPJ (Plena EA e Plena Tech) e CPF corretos. Commit `f411ad4`. Handoff antigo encerrado.

## 4. CEU — recibos (grande frente do dia)

- **Migration 073** ✅ aplicada: `entregas.numero_recibo`, `entregas.situacao`, sequência + RPC `proximo_numero_recibo()` (`REC-AAAA-NNNNN`).
- **Empresa real** do colaborador no recibo (era Plena EA fixa) — helper `src/lib/empresas.ts` (fallback Plena EA).
- **CA sempre no recibo**, vindo do `snapshot_item` — trocar CA no cadastro **não altera recibos antigos** (requisito de lote/CA novo).
- **Situação por item**: Novo, Substituição, Troca, Extravio/Perda — seletor no wizard, gravada na entrega, colorida no recibo. Item devolvido sai como **"Devolvido"**.
- **Número sequencial único** gravado na 1ª emissão; **reemissão reutiliza o mesmo número**.
- Regra "não excluir após recibo emitido": brecha do wizard fechada (agora marca `recibo_emitido`).
- **Lote corrigido:** emissão em lote busca **todas** as entregas do período no banco (antes filtrava só a página de 50).
- **Aba Relatórios:** botões "Gerar Recibo" e "Relatório em Lote" estavam **sem ação nenhuma** — ligados, respeitando os filtros da tela. Colunas **Situação** (entrega) e **Status** (Em aberto/Devolvido) separadas, inclusive no Excel.
- Lógica unificada em `src/lib/ceu/emissaoRecibos.ts` (Movimentações + Relatórios usam o mesmo módulo).
- Commits `95be845`, `685414b`.
- Limpeza: 6 entregas de teste (24/07) excluídas do banco a pedido da usuária.

## 5. Extras — falta sem extra + Reforço Contratual

- **Migration 074** ✅ aplicada: `extras.gera_extra` (default true) e `extras.reforco_contratual` (default false).
- **Migration 075** ✅ aplicada: RPC `registrar_extra_plantao` (usada pelo mobile) passa a gravar os 2 campos — a lista fixa de colunas os ignoraria silenciosamente.
- Categoria **"Faltista"** (R$ 0,00) cadastrada no banco — única que aceita valor zero.
- Formulário web: pergunta **"Gera extra para pagamento?"** no topo — "Não — falta (controle interno)" trava Faltista + R$0. Checkbox **Reforço Contratual** independente (faltista pode fazer reforço sem receber extra — combinação permitida; "sem extra" + "faturado" é bloqueada).
- **WhatsApp diário** (Balanço Operacional): faltas aparecem com R$ 0,00; marcador `🪙 *REFORÇO CONTRATUAL*` (💰 segue para faturado).
- **Recibos/pagamento:** `gera_extra=false` fica fora do agrupamento de pagamento.
- **Mobile (`/mobile/falta`) com paridade completa** (inspetores de fim de semana): mesma pergunta, toggle reforço, revisão com tipo de registro.
- Fix relacionado: `AutocompleteColaborador` não aceita mais texto livre silenciosamente ao sair do campo (bug do "patri" sem matrícula no campo ausente) — texto livre só via opção explícita "+ Usar ...".
- Commits `e69f957`, `57c1aa4`, `43345c0`.

## 6. Férias — últimas férias gozadas

- PDF "Previsão de Férias" (Flit, frente/verso) importado por **matrícula** via `scripts/importar-ferias-gozadas-pdf.cjs`: **125 períodos de gozo** preenchidos só para quem estava vazio (gozo = início + 29 dias, 30 dias CLT).
- 107 já tinham gozo (pulados); 3 com início 27/07/2026 já estavam como `agendado`; 84 sem data no PDF permanecem "Sem dados".
- Total na tabela: **254 períodos**. Casamento: 319 lidos, 0 não encontrados.
- ⚠️ O arquivo chegou em `public/` e foi movido para `dados-locais/` — **reforçar com a usuária: dados pessoais nunca em `public/`** (seriam publicados no site).

## 7. Estado do repositório e da infra

- **Migrations aplicadas hoje:** 073, 074, 075 (todas com doc `docs/APLICAR_MIGRATION_07X.md`).
- **Commits do dia:** `602039d` (limpeza public/), `2824cf6` (netlify-cli), `f411ad4` (PWA/CPF), `95be845` (CEU recibos), `685414b` (CEU relatórios), `e69f957` (extras), `43345c0` (férias + autocomplete), `57c1aa4` (extras mobile).
- **Deploy:** `npm run deploy:netlify` (build local + upload; zero crédito de build). Builds automáticos desligados.
- **Checks:** lint, build e testes passando (o 1 teste falho é o `rls.test.ts` — Python ausente no ambiente, documentado no AGENTS.md, não é falha real).
- **Modo YOLO** ativo no Kimi com regras `ask` para comandos destrutivos.

## 8. Pendências conhecidas (além do Netlify urgente)

- **Netlify credits** — ver §1. Prioridade absoluta.
- Ocorrências de teste antigas ("dddd", "gggg") — usuária já apagou; banco verificado limpo em 24/07.
- `departamento_id` da Graciane é NULL (só texto) — se criado o departamento, melhora fallback de empresa.
- Lista completa em `docs/CONTINUAR_AQUI.md` (revisar ocorrências do placeholder, testes manuais de perfis/storage/extras/escalas, refactors).
- `pdf.worker.min.mjs` (1,2 MB) e assets de `public/` sem header de cache imutável — avaliar se o Analytics apontar banda ali.

---

*Relatório escrito em 24/07/2026 ~18h. Usuária encerrou o dia cansada — retomar com calma e priorizando o §1.*
