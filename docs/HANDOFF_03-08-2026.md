# Handoff — 03/08/2026 (caso Mariana FECHADO: pareamento 12×36 nos dois sentidos)

> Passagem para o próximo agente. Sessão de 03/08. **Sem migration nova** (mudança só de frontend). Testes: **255 passando, 1 skipped** (rls.test, sem Python). Lint limpo, build ok. **Deploy Netlify produção FEITO em 03/08** (commit `9ddd859`, push no `main`, https://plena-corh.netlify.app — `/` e `/redefinir-senha` respondendo 200). Redirect URL de recuperação de senha já configurada no Supabase pela administradora.

## 1. Caso Mariana — diagnóstico real (o handoff de 01/08 estava errado)

- **O "bug do parser" (`nome=undefined`) nunca existiu.** O `parsePaginasEspelho` lê a página dela perfeitamente no espelho salvo (`20jun a 19jul (1).pdf`, pág. 223): nome, CPF, período e os 30 dias. O falso diagnóstico veio do script `scripts/inspecionar-espelho-mariana.ts`, que imprimia campos com nomes errados (`e.nome`/`dia.dataOriginal` em vez de `nomePdf`/`data`). **Script corrigido** — usar `npx tsx scripts/inspecionar-espelho-mariana.ts [NOME|CPF]` para inspeções futuras.
- **1º erro real: substituto nos dias errados.** O banco tinha o Marcelo nas FOLGAS dela (ímpares) em vez dos dias de escala. A pedido da usuária, o calendário dela foi zerado (backup em `dados-locais/backup_mariana_limpeza_calendario_2026-08-03.json`, script `scripts/limpar-calendario-mariana.mts`) e ela relançou pela tela, marcando o Marcelo nos dias em que ele REALMENTE trabalhou: 21, 23, 25, 27, 29/06, 01, 03, 04 e 07/07.
- **2º erro real (de regra):** o pareamento 12×36 só transferia a folga quando o dia coberto era dia de escala **do titular**. Como o Marcelo trabalhou no ritmo inverso (folgas dela), só 10 dias transferiam → relatório dava Mariana 20 / Marcelo 10.

## 2. Ajuste fino da regra 12×36 (validado com a gestão em 03/08/2026)

- **O par do 12×36 é (dia de escala, folga seguinte): se o substituto trabalhou QUALQUER dia do par, o par inteiro transfere** (cada dia apenas se estiver no bloco de férias/afastado). Dia de escala coberto → transfere a folga seguinte; folga coberta → transfere o dia de escala anterior. O ritmo do substituto NÃO precisa coincidir com a escala do titular.
- Implementação: `contarDiasTransferidos` em `src/lib/adicionais/calculoAdicionais.ts` (pareamento nos dois sentidos). Testes atualizados + caso real do Marcelo em `calculoAdicionais.test.ts` (28 testes no arquivo; 251 no total).
- `docs/REGRAS_NEGOCIO.md` atualizado com o ajuste fino.
- **Verificado com os dados reais do banco** (`scripts/simular-relatorio-mariana.mts`, usa as funções reais + estado atual): transferidos = 18 → **Mariana 12 / Marcelo 18 ✓**.
- Scripts de apoio criados: `scripts/estado-mariana.mts`, `scripts/estado-marcelo.mts`, `scripts/listar-espelhos.mts`, `scripts/simular-relatorio-mariana.mts`.

## 3. Estado final

- Banco: calendário da Mariana relançado pela usuária (30 linhas, Marcelo nos 9 dias trabalhados dele). Sem migration.
- `npm run lint` limpo · `npm test` 251 passando, 1 skipped · `npm run build` ok.
- **Deploy Netlify PENDENTE** — sem o deploy, o relatório em produção continua com a regra antiga (Mariana 20 / Marcelo 10).

## 4. Usuários reais criados (03/08/2026)

- 10 usuários criados via `scripts/criar-usuarios-reais.mts` (Auth Admin API, `email_confirm: true` + INSERT direto em `perfis` — o trigger de proteção da 064 é só BEFORE UPDATE, então INSERT via service role passa): Eliane/financeiro (gmail), Érica/financeiro, Maciel/mesa, Tayrone/inspetoria, Augusto/inspetoria, Rosely/rh, Ludmila/dp1, Elizabeth/dp2, Alexandre/gestor, Elisangela/adm.
- Senhas temporárias geradas (`Plena#XXXXXX`, sem caracteres ambíguos) — lista em `dados-locais/credenciais_usuarios_reais_2026-08-03.json` (gitignored). Troca pelo "Esqueci a senha".
- `eliane@plenafacilities.com.br` (conta anterior, visualizador) **elevada para adm** via DELETE+INSERT do perfil (UPDATE é bloqueado pelo trigger para service role); consentimento LGPD preservado.
- **"Esqueci a senha" implementado** (não existia UI — `recuperarSenha` estava órfão em `auth.ts`): link na `LoginPage`, nova `RedefinirSenhaPage` (rota `/redefinir-senha`, evento `PASSWORD_RECOVERY` tratado no `App.tsx`, após salvar encerra a sessão e volta ao login), validação em `src/lib/validacaoSenha.ts` (4 testes). **Pendente no dashboard Supabase:** adicionar `https://plena-corh.netlify.app/redefinir-senha` em Authentication → URL Configuration → Redirect URLs (só a administradora tem acesso).
- Perfis de teste (Administrador Teste, RH Teste etc., sem e-mail) continuam no banco — avaliar remoção.

## 5. Ajustes pré-deploy (03/08, tarde)

- **CEU — item novo "não aparecia" (item 1004):** não era bug de gravação — o item estava no banco (130 itens, query do app retorna todos). Causa: filtro persistido (`useFiltroPersistente`, desde 01/08) esquecido na listagem escondia o recém-criado. Correção: ao salvar item NOVO, o form navega com `state.itemCriado` e a listagem limpa os filtros (`CeuItemFormPage`/`CeuItensPage`). Scripts de diagnóstico: `scripts/inspecionar-item-1004.mts`, `scripts/simular-listagem-itens-ceu.mts`.
- **Ordenação A→Z (pedido da gestão):** (a) Calendário: novo controle "Ordenar por" (Colaborador/Departamento/Adicional) + botão A→Z/Z→A nos cartões de vínculo, persistido (`adicionais.calendario.ordenacao`); (b) Relatório: cabeçalhos clicáveis com setinha em Colaborador, Departamento e nas 5 colunas de adicional (texto A→Z; números começam maior→menor), persistido (`adicionais.relatorio.ordenacao`).

## 6. Incidente: deploy no site errado + redefinição de senha (03/08, fim do dia)

- **⚠️ Existem DOIS sites Netlify na conta:** `plena-corh` (PRODUÇÃO, id `2a90aecb-278e-4472-b7ff-b07dc521ce25`) e `sweet-nasturtium-fa7dc4` (id `f0b43221...`, não usar). O `.netlify/state.json` estava linkado no sweet-nasturtium e o primeiro deploy de 03/08 foi para ele por engano — produção continuou velha (tela sem "Esqueci a senha"). Corrigido com `npx netlify deploy --prod --dir=dist --site 2a90aecb-...` + `netlify link` no site certo. **Sempre conferir o hash do bundle em produção após o deploy** (`curl -s https://plena-corh.netlify.app/ | grep -o 'assets/index-[^"]*\.js'` vs `dist/index.html`).
- `scripts/redefinir-senha-usuario.mts <email>` — redefine a senha de um usuário para uma temporária nova (Admin API). Usado em `eliane@plenafacilities.com.br` (a usuária tentou logar nela com a senha da conta gmail — são duas contas da mesma pessoa: gmail=financeiro, facilities=adm).

## 7. Transferência de autoria + exclusão dos usuários de teste (03/08, noite)

- **Contexto:** os 9 perfis de teste eram usados pelas próprias pessoas que receberam login real — nada podia se perder, inclusive a autoria. Varredura definitiva (information_schema via CLI) achou 17 colunas com referência a usuário; com dados: `log_auditoria` 2.600, `entregas` 255 (DP2 Teste!), `locais_trabalho_diario.usuario_confirmacao_id` 73 (Mesa), `extras` 22, `historico_importacoes_econtador` 8, `projetos_vr` 4, `consentimentos_lgpd` 6.
- **Mapeamento confirmado pela gestão:** DP1→Ludmila, DP2→Elizabeth, Mesa→Maciel, Gestor→Alexandre, Financeiro→**Érica**, Inspetoria→**Augusto**, RH→Rosely. Administrador/Visualizador Teste: sem atividade, só excluídos.
- **Execução:** backup manifest em `dados-locais/backup_transferencia_usuarios_teste_2026-08-03.json` (2.962 registros: id + usuário antigo/novo — permite rollback); UPDATEs via `supabase db query --linked` (um por tabela, CASE com `::uuid` — multi-statement com BEGIN/COMMIT **não persiste** via Management API; PostgREST estoura timeout no log_auditoria). Verificação: 0 referências restantes (exceto os 6 consentimentos LGPD de teste, apagados com as contas — cada usuário real consente no 1º login da conta nova).
- **Exclusão:** `scripts/excluir-usuarios-teste.mts` apagou perfil + Auth dos 9. Estado final: 12 perfis (10 novos + financeiroplenaserv + eliane@plenafacilities).
- Scripts: `scripts/verificar-usuarios-teste.mts`, `scripts/varredura-refs-usuarios-teste.mts`, `scripts/transferir-autoria-usuarios-teste.mts` (PostgREST — timeout no log_auditoria; SQL via CLI foi o caminho), `scripts/excluir-usuarios-teste.mts`.

## 8. Pendências

- Validar com a usuária em produção: Relatório de Adicionais de julho deve mostrar **Mariana 12 / Marcelo 18** (o card "Direito no período" do Calendário usa a mesma função — também deve mostrar 12); testar o fluxo de recuperação de senha de ponta a ponta (Redirect URL já configurada no Supabase em 03/08); conferir a ordenação A→Z no Calendário e no Relatório; cadastrar um item novo no CEU e confirmar que ele aparece na listagem.
- Distribuir as senhas temporárias aos 10 usuários (ver seção 4 — orientação de comunicação foi passada à administradora); confirmar primeiro login de cada um (consentimento LGPD será exigido no primeiro acesso). Após todos logarem, avaliar remoção dos perfis de teste.
- O mistério da tela dela em 01/08 à noite (férias só 20–29/06) não se reproduziu — provavelmente ela usou um arquivo local diferente do espelho salvo. Sem ação no código.
- Pendências antigas: validações de produção (feriados, assinatura preta, "Lançado em", financeiro em ocorrências), recibo do Ricardo, importação unificada (~60 ocorrências), revisão visual dos menus por perfil, casos ambíguos da varredura anti-falso-sucesso, achados de borda da auditoria da regra (vínculo duplicado na chave, pareamento com data_inicio vazio).
