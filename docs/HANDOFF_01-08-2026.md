# Handoff — 01/08/2026 (anti-falso-sucesso nos hooks + módulo Relatórios)

> Passagem para o próximo agente. **Mudanças commitadas?** Verificar `git status` — ao escrever este handoff as alterações estavam prontas e verificadas (lint/testes/build verdes). Deploy em produção **não** feito (regra: agrupar 1–2 deploys/dia).

## 1. Segunda passada anti-falso-sucesso nos hooks (pendência antiga, ZERADA)

Varredura de todos os hooks restantes aplicando o padrão canônico: UPDATE/DELETE bloqueado por RLS retorna 0 linhas SEM erro → toda escrita agora termina em `.select('id')` e checa linhas afetadas antes do toast de sucesso.

**Corrigidos (15 arquivos):**
- `useCEUEntregas` — `devolver`, `marcarReciboEmitido`, `marcarLoteReciboEmitido` (lote: exige `data.length === ids.length`), `registrarEmissaoRecibo` (idem), `remover`.
- `useCEUFornecedores` — `atualizar`, `remover`. `useCEUItens` — `atualizar`, `remover`.
- `useAnexos` — `removerAnexo`. `useAlertas` — `marcarComoLido`, `arquivar`.
- `useOcorrencias` — `excluir`. `useTestemunhas` — `removerTestemunha` (item não some mais da tela em falso sucesso).
- `useColaboradores` — `upsertPorMatricula` (branch UPDATE; falha via `throw`, coerente com a assinatura).
- `useEscalasDiario` — `confirmarManual`, `aplicarEmLote`. `useEscalasLocais` — `atualizar`, `remover`. `useEscalasMapeamento` — `remover`.
- `useFerias` — baixa de previsões na importação agora conta as linhas realmente deletadas (antes podia reportar baixa fantasma).
- `useProjetosVR` — `excluir`. `usePermissoes` — `salvarPermissoes` (upsert em lote checa 0-linhas).

**Já protegidos (não mexidos):** `useExtras`, `useAdicionaisContratuais`, `useDepartamentos.atualizar/remover`, `useEmpresas`, `useFerias.excluirPeriodo`, `useProjetosVR.atualizar` (`.single()` gera erro natural), `useConfiguracaoVR` (só SELECT), `useResultadosVR`/`useExtrasRecibos`/`useEContador` (RPCs ou INSERTs — INSERT bloqueado por RLS retorna erro 42501, não precisa do padrão).

**Casos ambíguos deixados de propósito (registrar se virarem problema):**
- Falha **parcial** em lote não é detectada em `useEscalasDiario.aplicarEmLote` e `usePermissoes.salvarPermissoes` (só o caso 0-linhas).
- `useEscalasDiario.importarExcelFlit` — upsert em lote: a parte UPDATE bloqueada por RLS some em silêncio (toast pode superestimar importados).
- `useAnexos.removerAnexo` — o arquivo do storage é apagado ANTES do delete no banco; se o delete for bloqueado, o arquivo já se foi e a linha fica órfã (ordem pré-existente, não alterada).
- `useDepartamentos.sincronizar` — caminho só-mock (morto em produção).

## 2. Módulo `/relatorios` fora do placeholder

- Nova página `src/pages/RelatoriosPage.tsx`: hub de atalhos para os relatórios/exportações já existentes, agrupados por módulo — Extras (Relatório Semanal, Balanço Operacional, Recibos), Adicionais (Relatório), CEU (Relatórios), Férias (Painel CLT), Escalas (Local de Trabalho Diário), VR (Projetos/Comprovantes) e Gestão (Auditoria).
- Cada card só aparece se o usuário passar na **mesma permissão de rota da tela de destino** (`verificarPermissao(perfil, 'rota', acao)`) — nenhum card leva a uma tela bloqueada. Se nenhum card for visível, mostra `EmptyState`.
- Rota agora protegida: `ProtectedRoute` com `rota.relatorios` (antes era placeholder aberto). Registrada em `src/routes/lazyPages.ts`; `PlaceholderPage` não é mais usada no `App.tsx` (arquivo do componente mantido).
- O menu "Relatórios" da Sidebar (`menu.relatorios`) já existia e agora leva a uma tela real.

## 3. Limpeza

- `recibo_extra_teste_silva_2026-07-24.pdf` (pendência do handoff 31/07): **já não existe mais na raiz** — nada a fazer. Raiz está limpa.

## Estado final

- `npm run lint` — limpo · `npm test` — **222 passando, 1 skipped** (rls.test, sem Python) · `npm run build` — ok
- Nenhuma migration nova; nada a aplicar no banco.
- Convenção nova registrada no `AGENTS.md` (seção 12): update/delete sempre com `.select('id')` + checagem de linhas.

## Pendências (inalteradas das anteriores + novas)

- **Deploy desta sessão ainda não feito** — agrupar com a próxima mudança (15 créditos).
- Validar em produção (do handoff 31/07): feriados (cadastrar 24/06 e conferir coluna), assinatura preta na 1ª tentativa, Detalhes do recibo, "Lançado em". Lembrar Ctrl+Shift+R (PWA).
- Recibo do Ricardo (assinatura cinza histórica): avaliar remover e reemitir.
- Validação da importação unificada completa (~60 ocorrências) e busca de inativos na tela Ocorrências.
- Revisão visual dos menus por perfil (única parte manual da bateria de testes).
- Casos ambíguos da seção 1 acima (só se virarem problema real).
